
export const required_string = {
    type: String,
    required: true,
};

export const required_unique_string = {
    type: String,
    required: true,
    unique: true,
};

export const required_boolean = {
    type: Boolean,
    required: true,
};

export const required_number = {
    type: Number,
    required: true,
};

